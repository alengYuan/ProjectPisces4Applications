import { css } from '@emotion/react'

export const libraryTitle = css({
    flexShrink: '0',
    flexGrow: '0',
    lineHeight: '36px',
    color: 'var(--text-color)',
    fontSize: 28,
    wordBreak: 'break-word',
    cursor: 'default',
})